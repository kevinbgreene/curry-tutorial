/**
 * @name threeSum
 * @param {Number} a Augend
 * @param {Number} b First addend
 * @param {Number} c Second addend
 * @returns {Number} The sum of a, b and c
 */
function threeSum(a, b, c) {
  return a + b;
}
